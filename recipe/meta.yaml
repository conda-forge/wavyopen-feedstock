{% set name = "wavyopen" %}
{% set version = "0.5.9" %}
{% set python_min = "3.12" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 0adabda0fba0e9d5f0282b4a7e5bdc823e8f3cb71ea29743852e87ccb118a1c5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  entry_points:
    - wavyDownload = wavy.apps.wavyDownload:main
    - wavyCFG = wavy.apps.wavyCFG:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - matplotlib-base >=3.5
    - numpy >=1.20
    - scipy <1.17
    - netcdf4 >=1.6
    - pyyaml >=6
    - cmocean >=3
    - joblib >=1.4
    - pandas >=2.2
    - xarray >=2024
    - pyresample >=1.30
    - geojson >=2.5
    - cartopy >=0.20
    - pygam >=0.9
    - roaring-landmask >=0.10
    - copernicusmarine >=2.0
    - xdg
    - tqdm >=4
    - python-dotenv
    - click
    - scikit-learn >=1.6
    - yapf
    - aiohttp
    - fsspec
    - requests
    - dill
    - geopandas

test:
  imports:
    - wavy
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check

about:
  home: https://github.com/bohlinger/wavy
  summary: 'A package for processing/calibrating/validating wave measurements and wave model output.'
  description: A package for processing/calibrating/validating wave measurements and wave model output.
  license: MIT
  license_family: MIT
  license_file: LICENSE
  doc_url: https://wavyopen.readthedocs.io
  dev_url: https://github.com/bohlinger/wavy

extra:
  recipe-maintainers:
    - bohlinger
    - gauteh
